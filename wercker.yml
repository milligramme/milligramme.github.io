box: debian

build:
    steps:
        - arjen/hugo-build:
            version: "HEAD"
            config: config.toml
            disable_pygments: true
deploy:
    steps:
        - lukevivier/gh-pages@0.2.1:
            token: $GITHUB_TOKEN
            basedir: public